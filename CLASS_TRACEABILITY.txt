CLASS TRACEABILITY (Class Diagram → Real Code Locations)
Project: Poultry Monitoring System
Generated: 2026-05-07

Notes
- Some “classes” in `class.mmd` are conceptual UML blocks. In Python, many are implemented as:
  - Flask route functions
  - service/helper functions
  - dictionaries (inputs/results) rather than explicit dataclasses
- Line numbers below refer to the current files in this repo.

================================================================================
1) PoultryMonitoringSystem (conceptual system root)
--------------------------------------------------------------------------------
Implementation
- File: `app.py`
- Flask app initialization + blueprint registration:
  - Lines 1–37 (imports, dotenv, create app, register `financial_bp` and `chatbot_bp`)

Key relations
- “contains FinancialModule / ChatbotModule / DiseaseDetectionModule / WeightEstimationModule”
  - Implemented by: route handlers in `app.py`, plus Flask blueprints for finance/chatbot.

================================================================================
2) FinancialModule (startup planner + dashboard)
--------------------------------------------------------------------------------
Implementation
- File: `financial/routes.py`
- Blueprint: `financial_bp` (line 28)
- Startup planner routes:
  - `startup()` starts at line 126
  - `startup_calculate()` starts at line 1045
  - `startup_template()` starts at line 1454
- Existing-farm dashboard routes:
  - `dashboard_financial()` starts at line 1470
  - `dashboard_analyze()` starts at line 1521

Key relations
- uses FinancialCalculator → `financial/calculations.py` (functions imported at `financial/routes.py` lines 5–13)
- uses PriceForecastService → `train_and_forecast_prophet()` imported at `financial/routes.py` lines 18–20
- uses WeatherService → `build_weather_based_recommendations()` imported at `financial/routes.py` line 17
- reads MarketPriceService → `get_today_price()/get_today_chick_price()/get_today_feed_prices()` imported at `financial/routes.py` lines 15–16
- provides ChatContext → `build_startup_chatbot_context()` imported at `financial/routes.py` line 21

================================================================================
3) StartupPlanInput (conceptual data object)
--------------------------------------------------------------------------------
Implementation (as dict)
- File: `financial/routes.py`
- Defaults dict with keys matching the UML attributes:
  - `DEFAULT_STARTUP_VALUES` lines 81–99
- Runtime request values:
  - `form_values` (GET/default) created around lines 136–139
  - `values` (POST calculate) created in `startup_calculate()` around lines 1052+ (simple/advanced modes)

Validation
- Implemented via defensive parsing:
  - `_parse_float()` lines 40–47

================================================================================
4) StartupPlanResult (conceptual data object)
--------------------------------------------------------------------------------
Implementation (as dict)
- File: `financial/calculations.py`
- `calculate_startup_plan(...)` lines 13–100 returns dict:
  - `live_chickens`, `total_weight`, `revenue`, `total_cost`, `profit`, `roi`,
    `break_even_price`, `cost_breakdown`

Rendered on UI
- File: `templates/financial_startup.html`
- (Cards/insights/decision blocks render values from `results` and `form_values`)

================================================================================
5) FinancialCalculator (financial logic services)
--------------------------------------------------------------------------------
Implementation
- File: `financial/calculations.py`
Core:
- `calculate_startup_plan(...)` lines 13–100
- `analyze_existing_farm(...)` lines 103–177
Charts/support:
- `build_profit_sensitivity_series(...)` starts line 180
- `build_mortality_impact_series(...)` (later in file)
Decision simulations:
- `simulate_best_selling_time(...)` starts line 281
- `simulate_decision_curves(...)` starts line 349

================================================================================
6) PriceForecastService + PriceForecastRow (Prophet forecast)
--------------------------------------------------------------------------------
Implementation
- File: `poultry_price_forecasting_prophet.py`
- Forecast horizon constants and Prophet pipeline defined at top of file.
- Main entry used by the platform:
  - `train_and_forecast_prophet()` lines 503–516
  - Returns forecast rows with columns: `date`, `low_price`, `expected_price`, `high_price`

Integration point
- File: `financial/routes.py`
- Import: `train_and_forecast_prophet` at lines 18–20
- Used inside startup/dashboard analysis to build decision curves and tables.

================================================================================
7) WeatherService + WeatherCandidate (weather-based risk)
--------------------------------------------------------------------------------
Implementation
- File: `financial/weather.py`
- Dataclass:
  - `WeatherCandidate` lines 102–116
- Main function:
  - `build_weather_based_recommendations(...)` starts line 370
- Data providers:
  - Visual Crossing + Open-Meteo fallback (same file; fetch helpers above)

Integration point
- File: `financial/routes.py`
- Import at line 17 and used during startup + dashboard logic.

================================================================================
8) MarketPriceService (broiler/chick/feed prices)
--------------------------------------------------------------------------------
Implementation
- File: `financial/market_price.py`
- `get_today_price()` starts line 208
- `get_today_chick_price()` starts line 240
- `get_today_feed_prices()` starts line 266
- `get_today_prices_snapshot()` starts line 334

Integration point
- File: `financial/routes.py`
- Imports at lines 15–16; used to auto-fill defaults and dashboard cards.

================================================================================
9) ChatbotModule + ChatContext + ChatTurn + GeminiClient (Gemini assistant)
--------------------------------------------------------------------------------
Implementation
- File: `chatbot_routes.py`
Context builders:
- `build_startup_chatbot_context(...)` starts line 315
- `build_dashboard_chatbot_context(...)` starts line 428

Model catalog + call:
- `fetch_gemini_models_catalog(...)` starts line 480
- `chat_models_list()` starts line 600
- `chat()` starts line 628

Client implementation detail
- “GeminiClient” is implemented as internal helper functions:
  - `_resolve_model_chain()` (earlier in file)
  - `_call_gemini(...)` (earlier in file)
  - plus `fetch_gemini_models_catalog(...)`

ChatTurn (history)
- Implemented as a list of dict turns:
  - Frontend: `static/js/chatbot.js` keeps `history` and sends it to `/api/chat`
  - Backend: `chatbot_routes.py` parses `history` in `chat()` and appends to Gemini `contents`

================================================================================
10) DiseaseDetectionModule (broiler & fecal disease inference)
--------------------------------------------------------------------------------
Implementation
- File: `app.py`
Models:
- `BroilerDiseasePredictor.predict(...)` starts at line 352
- `FecalDiseasePredictor.predict(...)` starts at line 477 (note: file contains duplicated method blocks)
Routes:
- Page route: `/disease` handler lines 589–591
- API route (broiler): `/analyze-broiler-disease` starts line 912
- API route (fecal): `/analyze-fecal-disease` starts line 1032

================================================================================
11) WeightEstimationModule (image/video weight estimation)
--------------------------------------------------------------------------------
Implementation
- File: `app.py`
Page route:
- `/weight` handler lines 593–595
API routes:
- `/estimate-weight` starts line 1494
- `/estimate-weight-video` starts line 1276
Helpers:
- `extract_weight_value(...)` starts line 670

================================================================================
12) UMLChecklist (presentation-only)
--------------------------------------------------------------------------------
Implementation
- Not implemented in code; included only for UML presentation completeness.

